default: &default
  adapter: postgresql
  encoding: unicode
  username: <%= ENV['POSTGRES_USER'] || 'postgres'%>
  password: <%= ENV['POSTGRES_PASSWD'] || 'postgres' %>
  host: <%= ENV['POSTGRES_HOST'] || 'localhost' %>
  port: 5432
  pool: 5

development:
  <<: *default
  database: coolify_development

test:
  <<: *default
  database: coolify_test

production:
  <<: *default
  database: coolify_production
